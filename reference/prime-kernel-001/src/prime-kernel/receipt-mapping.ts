// Legacy compatibility note:
// PRIME-KERNEL-001 v0.2 uses mus-receipt.ts for signed, hash-linked receipts.
// This module remains intentionally empty so older import paths do not imply a second receipt system.
export {};
